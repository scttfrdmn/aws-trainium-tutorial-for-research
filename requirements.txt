# Core AWS and ML dependencies
boto3>=1.34.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.30.0

# Neuron SDK — install from the AWS Neuron pip index, NOT from PyPI.
# torch / torch-xla are pulled in (and pinned) by torch-neuronx, so we deliberately do NOT pin
# torch here — let the Neuron wheel resolve a matched version. See VERSION_MATRIX.md.
#   pip install torch-neuronx neuronx-cc \
#       --extra-index-url https://pip.repos.neuron.amazonaws.com
# As of Neuron SDK 2.30.0 this is the PyTorch 2.9 / XLA path. PyTorch 2.10+ will use the native
# TorchNeuron backend (private preview).

# Data processing and analysis
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Web and API frameworks
flask>=2.3.0
requests>=2.31.0

# Scientific computing
scipy>=1.10.0

# Chemistry and biology (for domain examples)
rdkit>=2023.3.1
biopython>=1.81

# Vector databases and search
faiss-cpu>=1.7.4

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
click>=8.1.0
psutil>=5.9.0
python-dateutil>=2.8.2

# Development and testing
pytest>=7.0.0
black>=26.5.1
flake8>=6.0.0
mypy>=1.0.0

# Optional: Jupyter for notebooks
jupyter>=1.0.0
ipywidgets>=8.0.0